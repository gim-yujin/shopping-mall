# Gradle/Spring build outputs
build/
!gradle/wrapper/gradle-wrapper.jar
*.jar
.gradle/
# Do not upload local data/experiment directories
csv_data/
experiment_results/
optimization/
# load-test/ 내부 .gitignore에서 raw 데이터만 제외
# Log files
*.log
# Local env files
.env
# 로컬 개발 프로파일 (DB 자격증명 포함)
src/main/resources/application-local.yml
# Local compressed files
*.zip
# WAL data directory (runtime-generated, not source)
data/
# Codex local workspace marker
.codex
